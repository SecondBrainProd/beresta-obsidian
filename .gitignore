# Собранный плагин. В репозиторий не кладётся: три файла релиза
# (`main.js`, `manifest.json`, `styles.css`) собираются `make build-plugin`
# из исходников, которые здесь и лежат. Собранный файл в репозитории — это
# вторая правда о том, что делает плагин, и рано или поздно она разойдётся
# с первой.
main.js
main.js.map

# Замер веса Nunjucks и прочие временные сборки (`esbuild.config.mjs --out`).
tmp/
